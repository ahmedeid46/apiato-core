<?php

namespace App\Containers\{{section-name}}\{{container-name}}\Actions;

use App\Containers\{{section-name}}\{{container-name}}\Models\{{model}};
use App\Containers\{{section-name}}\{{container-name}}\Tasks\Create{{model}}Task;
@if($ui === 'api' || $ui === 'both')
use App\Containers\{{section-name}}\{{container-name}}\UI\API\Requests\Create{{model}}Request as Create{{model}}ApiRequest;
@endif

@if($ui === 'web' || $ui === 'both')
use App\Containers\{{section-name}}\{{container-name}}\UI\WEB\Requests\Create{{model}}Request as Create{{model}}WebRequest;
@endif
use App\Ship\Parents\Actions\Action as ParentAction;

final class {{class-name}} extends ParentAction
{
    public function __construct(
        private readonly Create{{model}}Task $create{{model}}Task,
    ) {
    }

    @if($ui === 'api')
    public function run(Create{{model}}ApiRequest $request): {{model}}
    {
        $data = $request->sanitize([
            // add your request data here
        ]);

        return $this->create{{model}}Task->run($data);
    }
    @elseif($ui === 'web')
    public function run(Create{{model}}WebRequest $request): {{model}}
    {
        $data = $request->sanitize([
            // add your request data here
        ]);

        return $this->create{{model}}Task->run($data);
    }
    @else
    public function run(Create{{model}}ApiRequest|Create{{model}}WebRequest $request): {{model}}
    {
        $data = $request->sanitize([
            // add your request data here
        ]);

        return $this->create{{model}}Task->run($data);
    }
    @endif
}
