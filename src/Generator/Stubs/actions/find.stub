<?php

namespace App\Containers\{{section-name}}\{{container-name}}\Actions;

use App\Containers\{{section-name}}\{{container-name}}\Models\{{model}};
use App\Containers\{{section-name}}\{{container-name}}\Tasks\Find{{model}}ByIdTask;
@if($ui === 'api' || $ui === 'both')
use App\Containers\{{section-name}}\{{container-name}}\UI\API\Requests\Find{{model}}ByIdRequest as Find{{model}}ByIdApiRequest;
@endif

@if($ui === 'web' || $ui === 'both')
use App\Containers\{{section-name}}\{{container-name}}\UI\WEB\Requests\Find{{model}}ByIdRequest as Find{{model}}ByIdWebRequest;
@endif
use App\Ship\Parents\Actions\Action as ParentAction;

final class {{class-name}} extends ParentAction
{
    public function __construct(
        private readonly Find{{model}}ByIdTask $find{{model}}ByIdTask,
    ) {
    }

    @if($ui === 'api')
    public function run(Find{{model}}ByIdApiRequest $request): {{model}}
    {
        return $this->find{{model}}ByIdTask->run($request->id);
    }
    @elseif($ui === 'web')
    public function run(Find{{model}}ByIdWebRequest $request): {{model}}
    {
        return $this->find{{model}}ByIdTask->run($request->id);
    }
    @else
    public function run(Find{{model}}ByIdApiRequest|Find{{model}}ByIdWebRequest $request): {{model}}
    {
        return $this->find{{model}}ByIdTask->run($request->id);
    }
    @endif
}
