<?php

namespace App\Containers\{{section-name}}\{{container-name}}\Actions;

use App\Containers\{{section-name}}\{{container-name}}\Models\{{model}};
use App\Containers\{{section-name}}\{{container-name}}\Tasks\Update{{model}}Task;
@if($ui === 'api' || $ui === 'both')
use App\Containers\{{section-name}}\{{container-name}}\UI\API\Requests\Update{{model}}Request as Update{{model}}ApiRequest;
@endif

@if($ui === 'web' || $ui === 'both')
use App\Containers\{{section-name}}\{{container-name}}\UI\WEB\Requests\Update{{model}}Request as Update{{model}}WebRequest;
@endif
use App\Ship\Parents\Actions\Action as ParentAction;

final class {{class-name}} extends ParentAction
{
    public function __construct(
        private readonly Update{{model}}Task $update{{model}}Task,
    ) {
    }

    @if($ui === 'api')
    public function run(Update{{model}}ApiRequest $request): {{model}}
    {
        $data = $request->sanitize([
            // add your request data here
        ]);

        return $this->update{{model}}Task->run($data, $request->id);
    }
    @elseif($ui === 'web')
    public function run(Update{{model}}WebRequest $request): {{model}}
    {
        $data = $request->sanitize([
            // add your request data here
        ]);

        return $this->update{{model}}Task->run($data, $request->id);
    }
    @else
    public function run(Update{{model}}ApiRequest|Update{{model}}WebRequest $request): {{model}}
    {
        $data = $request->sanitize([
            // add your request data here
        ]);

        return $this->update{{model}}Task->run($data, $request->id);
    }
    @endif
}
